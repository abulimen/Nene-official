# API Configuration
VITE_API_URL=http://localhost:5000/api

# Uploads URL (where images/files are served from)
# Only needed if uploads are served from a different domain than the API
# VITE_UPLOADS_URL=http://localhost:5000

# Production example:
# VITE_API_URL=https://api.yourdomain.com/api
# VITE_UPLOADS_URL=https://api.yourdomain.com

VITE_GOOGLE_CLIENT_ID=